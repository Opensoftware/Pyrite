<h2> <%=t "header_edit_block_type" %> </h2>

<%= form_for @block_type, :url => block_type_path(@block_type) do |f| %>
  <%= error_msgs_for @block_type %>
  <%= render 'form', :f => f %>
<% end %>

<%= link_to 'Back', block_types_path, :class => 'back-btn' %>
